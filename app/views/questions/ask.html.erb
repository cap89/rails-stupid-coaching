<style>
  form {
    display: flex;
    flex-direction: column;
    gap: 10px;
    width: 100%;
    max-width: 400px; /* Adjust based on your preference */
    margin: auto;
    padding: 20px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    border-radius: 8px;
    background-color: #fff;
  }

  input[type="text"] {
    padding: 10px;
    font-size: 16px;
    border: 1px solid #ccc;
    border-radius: 4px;
    transition: border-color 0.3s;
  }

  input[type="text"]:focus {
    border-color: #007bff; /* Adjust the color to match your theme */
    outline: none;
  }

  input[type="submit"] {
    padding: 10px;
    font-size: 16px;
    color: #fff;
    background-color: #007bff; /* Adjust the background color to match your theme */
    border: none;
    border-radius: 4px;
    cursor: pointer;
    transition: background-color 0.3s;
  }

  input[type="submit"]:hover {
    background-color: #0056b3; /* Darker shade for hover effect */
  }

  /* Styling for h1 */
  h1 {
    color: #000; /* Black color for the h1 */
    font-family: Arial, sans-serif; /* Arial font */
    text-align: center; /* Center aligning the heading */
    margin-bottom: 20px; /* Adding some space below the heading */
  }

  /* Styling for h3 */
  h3 {
    color: #808080; /* Grey color for the h3 */
    font-family: Arial, sans-serif; /* Arial font */
    text-align: center; /* Center aligning the subheading */
    margin-bottom: 10px; /* Adjust spacing as per your layout */
  }
</style>

<h1>Stupid Coaching</h1>
<h3>Ask your coach anything!</h3>
<form action="/answer" data-turbo="false">
  <input type="text" name="answer">
  <input type="submit" value="Ask!">
</form>
